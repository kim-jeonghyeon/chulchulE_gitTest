pull > push


git add . (git 변경된 파일 있으면 수정)

git config --global user.email "~~"

git config --global user.name "~~"

git commit -m "~~"

git push origin master  (실제로 git에 push 할 수있도록 하는 명령어)

-------------------------------------

git init

git remote add origin 주소

git pull origin master

-------------------------------

git status (git 연결상태 확인)